# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Late API (will be Zernio)
LATE_API_KEY=your-late-api-key

# AI Provider (fallback for AI Response flow node - set per-workspace in settings)
# Supports OpenAI, Anthropic, and Google via Vercel AI SDK
AI_API_KEY=your-ai-api-key

# Cron secret (for sequence processor + job scheduler)
CRON_SECRET=your-cron-secret

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
